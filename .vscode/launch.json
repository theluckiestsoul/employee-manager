{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "env": {
                "DB_URL": "postgres://kiran:kiran@localhost:5678/employeemanager?sslmode=disable",
                "PORT":"8080"
            }
        }
    ]
}